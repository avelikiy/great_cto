<!-- auto-generated by scripts/generate-summary.mjs · mode=heuristic · src=PLAN-vertical-scorecard.md -->
# PLAN: Vertical quality scorecard (0–100) · summary

_Generated heuristically (no ANTHROPIC_API_KEY). For higher quality summaries, set ANTHROPIC_API_KEY._

- **Tier 0 (deterministic, $0, runs in CI):** dimensions 1 + 7. Hygiene, not proof of judgment.
- **Tier 1 (behavioural, LLM, ~$0.20–0.40 / vertical):** dimensions 2–6. The real signal. Needs
- **planted** (≥ 6) — ARCH stub with a baked-in violation → expect `BLOCKED` + ≥1 keyword.
- **benign** (≥ 3) — clean ARCH in the same vertical → expect no block / no findings (catches over-fir
- **adversarial** (≥ 3) — a case that argues the reviewer into passing (jailbreak the gate) → expect i

- **Full doc:** docs/plans/PLAN-vertical-scorecard.md
