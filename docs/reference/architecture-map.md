<!-- GENERATED by scripts/gen-docs-reference.mjs — do not edit by hand. -->

# Architecture maps

Both diagrams below are **derived**, not drawn: one from the import graph of
the code as it stands, the other from `shared/pipeline.toml` — the same file
the dispatcher acts on. A diagram that disagrees with the system is therefore
impossible rather than merely unlikely, and CI checks it by regenerating this
page and comparing.

There is no "generated on" date here on purpose. A date would change on every
run and make the drift check useless; the check is the freshness claim.

See also: [Agents](agents.md) · [Commands](commands.md) · [Skills](skills.md).

## How the code is arranged

Derived from what imports what, across 586 files in 9 groups.

```mermaid
flowchart TD
  contracts["Contracts<br/><small>3 files</small>"]
  agents["Agents<br/><small>70 files</small>"]
  commands["Commands<br/><small>44 files</small>"]
  skills["Skills<br/><small>151 files</small>"]
  hooks["Hooks<br/><small>25 files</small>"]
  libs["Libraries<br/><small>115 files</small>"]
  board["Board<br/><small>66 files</small>"]
  cli["CLI<br/><small>32 files</small>"]
  evals["Evals<br/><small>80 files</small>"]
  hooks -->|23| libs
  board -->|10| libs
  libs -->|3| hooks
  libs -->|1| evals
```

| Group | Files | What it holds |
|---|---:|---|
| `contracts` | 3 | the pipeline map, orchestrator rules |
| `agents` | 70 | the specialists the pipeline dispatches |
| `commands` | 44 | what a human can invoke directly |
| `skills` | 151 | knowledge agents load on demand |
| `hooks` | 25 | what fires on session, tool and stop events |
| `libs` | 115 | the logic hooks and commands share |
| `board` | 66 | the admin view, zero runtime dependencies |
| `cli` | 32 | the published npm package |
| `evals` | 80 | what each agent is measured against |

## How a feature moves

Derived from `shared/pipeline.toml`. Gates are drawn because they are
where a human stands in the flow, and a map of an automated pipeline that
hides its stopping points describes something other than what runs.

```mermaid
flowchart TD
  product_owner["product-owner"]
  architect["architect"]
  product_owner -->|gate:product| architect
  pm["pm"]
  architect -->|gate:arch| pm
  senior_dev["senior-dev"]
  pm -->|gate:plan| senior_dev
  code_reviewer["code-reviewer"]
  senior_dev -->|gate:code| code_reviewer
  qa_engineer["qa-engineer"]
  code_reviewer --> qa_engineer
  security_officer["security-officer"]
  code_reviewer --> security_officer
  devops["devops"]
  qa_engineer -->|gate:qa + gate:ship| devops
  security_officer -.->|join| qa_engineer
  security_officer -->|gate:security + gate:compliance + gate:ship| devops
  qa_engineer -.->|join| security_officer
  l3_support["l3-support"]
  devops --> l3_support
  project_auditor["project-auditor"]
  auth_engineer["auth-engineer"]
  auth_engineer --> senior_dev
  subscription_billing_engineer["subscription-billing-engineer"]
  subscription_billing_engineer --> senior_dev
  integrations_engineer["integrations-engineer"]
  integrations_engineer --> senior_dev
  connector_builder["connector-builder"]
  connector_builder --> senior_dev
  media_pipeline_engineer["media-pipeline-engineer"]
  media_pipeline_engineer --> senior_dev
  geo_routing_engineer["geo-routing-engineer"]
  geo_routing_engineer --> senior_dev
  mobile_app_builder["mobile-app-builder"]
  mobile_app_builder -->|gate:code| code_reviewer
  e2e_test_engineer["e2e-test-engineer"]
  e2e_test_engineer --> devops
  migration_import_engineer["migration-import-engineer"]
  migration_import_engineer -->|gate:import| senior_dev
  ai_prompt_architect["ai-prompt-architect"]
  ai_eval_engineer["ai-eval-engineer"]
  ai_prompt_architect --> ai_eval_engineer
  ai_eval_engineer --> senior_dev
  db_migration_reviewer["db-migration-reviewer"]
  db_migration_reviewer --> qa_engineer
  design_advisor["design-advisor"]
  design_advisor --> senior_dev
  performance_engineer["performance-engineer"]
  performance_engineer --> qa_engineer
  infra_provisioner["infra-provisioner"]
  infra_provisioner --> e2e_test_engineer
  growth_engineer["growth-engineer"]
  growth_engineer --> devops
```
