# Public REST + GraphQL API Platform

Developer portal exposing public REST API, GraphQL endpoint, and webhook subscriptions.
OpenAPI spec is the source of truth. SDK auto-generated for Node, Python, Go.
Per-tier rate limits, OAuth 2.1 + PKCE, signed webhooks.
