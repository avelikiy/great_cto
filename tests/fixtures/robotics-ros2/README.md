# Surgical Robot — ROS 2 + MoveIt

Autonomous surgical robot with ML perception. ROS 2 Humble + MoveIt 2 + Gazebo simulator.
Cobot architecture per ISO/TS 15066. E-stop independent path. SROS2 enforced.
Target: FDA SaMD Class III (PMA path).
